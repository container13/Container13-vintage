(function(){
'use strict';
const VERSION='0.2.32';
const KEY='lina_clean_swing_g3_forward_v0211';
const API='https://linas-opti-api.mangaj73.workers.dev';
const ANCHOR='2026-09-11',TRAIN_START='2020-01-01';
const COST_SIDE=.001,CAPITAL=100000,RISK=.005,MAXPOS=5,MAXPOSPCT=.20;
const PLAN_HASH='75838ed5';
const SYMBOLS=['AMD','SHOP','ADBE','MU','FDX','TSLA','LUV','NFLX','C','NOW','QCOM','BAC','GM','DDOG','PYPL','NVDA'];
const POOL=[
 ['ae0e95e3',55,'sma200',1.5,'spy200'],['8db4137e',55,'sma200',1.5,'spy100'],['077a688a',100,'sma200',1.5,'spy200'],['c99e523f',100,'sma200',1.5,'spy100'],['46b04309',55,'off',1.5,'spy200'],['fa2294a0',55,'sma100',1.5,'spy200'],['bb98f732',100,'off',1.5,'spy200'],['1f961809',100,'sma100',1.5,'spy200'],['574fa00e',20,'sma200',1.5,'spy100'],['d684a0fa',55,'sma200',1.5,'off'],['34a399c9',100,'sma200',1.5,'off'],['69147890',55,'off',1.5,'off']
].map(([hash,breakout,trend,volume,regime])=>({hash,p:{breakout,trend,volume,regime,stop:.07,target:.15,hold:10}}));
const SEPT_MODEL={month:'2026-09',trainEnd:'2026-08-31',selectedHash:'69147890',params:{breakout:55,trend:'off',volume:1.5,regime:'off',stop:.07,target:.15,hold:10},source:'G3 frozen PASS'};
function today(){return new Date().toISOString().slice(0,10)}
function load(){try{return JSON.parse(localStorage.getItem(KEY)||'null')}catch{return null}}
function fresh(){return{schema:'LINA-G3-REAL-FORWARD-1',version:'V0.2.32',planHash:PLAN_HASH,anchor:ANCHOR,costSide:COST_SIDE,capital:CAPITAL,createdAt:new Date().toISOString(),models:[SEPT_MODEL],closed:[],open:[],stats:null,lastMarketDate:null,lastRefreshAt:null,provider:null,milestones:{60:false,120:false,250:false},history:[]}}
function save(x){x.savedAt=new Date().toISOString();localStorage.setItem(KEY,JSON.stringify(x));document.dispatchEvent(new CustomEvent('lina:g3forwardchange'));return x}
function rowsOf(j){return Array.isArray(j)?j:(j?.rows||j?.data||[])}
function norm(rows,symbol,provider){return rows.map(r=>({symbol:String(r.symbol||r.s||symbol).replace(/\.US$/,'').toUpperCase(),t:String(r.t||r.time||r.timestamp||''),o:+r.o,h:+r.h,l:+r.l,c:+r.c,v:+(r.v||0),provider})).filter(r=>r.symbol&&r.t&&[r.o,r.h,r.l,r.c].every(Number.isFinite))}
async function getSymbol(symbol,start,end){const tries=[['Worker Yahoo daily',`/yahoo-bars?symbols=${encodeURIComponent(symbol)}&timeframe=1Day&start=${start}&end=${end}`],['EODHD .US',`/eod-bars?symbols=${encodeURIComponent(symbol+'.US')}&timeframe=1Day&start=${start}&end=${end}`],['Alpaca daily',`/bars?symbols=${encodeURIComponent(symbol)}&timeframe=1Day&start=${start}T00:00:00Z&end=${end}T23:59:59Z`]];let errors=[];for(const [name,path] of tries){try{const r=await fetch(API+path,{cache:'no-store'});let j={};try{j=await r.json()}catch{}if(!r.ok)throw new Error(j.error||('HTTP '+r.status));const a=norm(rowsOf(j),symbol,name);if(a.length)return a;throw new Error('0 dagsrader')}catch(e){errors.push(name+': '+(e.message||e))}}throw new Error(symbol+' – '+errors.join(' | '))}
function dateOf(r){return String(r.t).slice(0,10)}
function prep(rows){const by={};for(const r of rows)(by[r.symbol]??=[]).push(r);for(const a of Object.values(by))a.sort((x,y)=>x.t.localeCompare(y.t));return by}
function cut(rows,end,start=TRAIN_START){return rows.filter(r=>{const d=dateOf(r);return d>=start&&d<=end})}
function sma(a,i,n){if(i-n+1<0)return null;let s=0;for(let k=i-n+1;k<=i;k++)s+=a[k].c;return s/n}
function avgVol(a,i,n=20){if(i-n+1<0)return null;let s=0;for(let k=i-n+1;k<=i;k++)s+=a[k].v||0;return s/n}
function prevHigh(a,i,n){if(i-n<0)return null;let h=-Infinity;for(let k=i-n;k<=i-1;k++)h=Math.max(h,a[k].c);return h}
function engine(rows,p){return window.LinaG2Engine.engine(rows,p)}
function statsYears(rows,p){return[...new Set(rows.map(r=>dateOf(r).slice(0,4)))].sort().map(year=>{const r=engine(cut(rows,year+'-12-31',year+'-01-01'),p);return{year,n:r.n,pl:r.pl,pf:r.pf,wr:r.wr,dd:r.dd}})}
function score(r,ys){if(!r||r.n<30||!Number.isFinite(r.pf))return-1e12;const positive=ys.filter(y=>y.pl>0).length,worst=Math.min(...ys.map(y=>y.pl)),ratio=ys.length?positive/ys.length:0;return(r.pf-1)*100+ratio*24+Math.min(20,r.n/12)+r.dd*160+Math.min(0,worst/600)}
function lastDateBefore(rows,month){const ds=[...new Set(rows.map(dateOf))].filter(d=>d<month+'-01').sort();return ds.at(-1)||null}
function monthsFromAnchor(rows){return[...new Set(rows.map(dateOf).filter(d=>d>=ANCHOR).map(d=>d.slice(0,7)))].sort()}
async function ensureModels(rows,x,progress){const have=new Set((x.models||[]).map(m=>m.month));for(const month of monthsFromAnchor(rows)){if(have.has(month))continue;const trainEnd=lastDateBefore(rows,month);if(!trainEnd)continue;const train=cut(rows,trainEnd),rank=[];for(let i=0;i<POOL.length;i++){const z=POOL[i],r=engine(train,z.p),ys=statsYears(train,z.p);rank.push({hash:z.hash,p:z.p,score:score(r,ys),n:r.n,pl:r.pl,pf:r.pf,dd:r.dd});progress?.(`G3 omträning ${month} · ${i+1}/${POOL.length}`);if(i%4===0)await new Promise(r=>setTimeout(r,0))}rank.sort((a,b)=>b.score-a.score);const b=rank[0];x.models.push({month,trainEnd,selectedHash:b.hash,params:b.p,score:b.score,train:{n:b.n,pl:b.pl,pf:b.pf,dd:b.dd},source:'real-forward'});have.add(month);save(x)}}
function calcStats(closed,open,lastPrices){const n=closed.length,w=closed.filter(t=>t.pnl>0),l=closed.filter(t=>t.pnl<0),gp=w.reduce((s,t)=>s+t.pnl,0),gl=-l.reduce((s,t)=>s+t.pnl,0),pl=closed.reduce((s,t)=>s+t.pnl,0);let eq=CAPITAL,peak=CAPITAL,dd=0;for(const t of closed.slice().sort((a,b)=>a.exitDate.localeCompare(b.exitDate))){eq+=t.pnl;peak=Math.max(peak,eq);dd=Math.min(dd,eq/peak-1)}const unrealized=open.reduce((s,q)=>{const px=lastPrices[q.symbol]||q.entryRaw;return s+q.shares*px-q.cost},0);return{n,pl,pf:gl?gp/gl:(gp?Infinity:0),wr:n?w.length/n:0,dd,unrealized,open:open.length,equity:CAPITAL+pl+unrealized}}
function simulate(rows,models){const by=prep(rows),spy=by.SPY||[],symbols=SYMBOLS.filter(s=>by[s]?.length),dates=[...new Set(rows.map(dateOf))].sort(),maps={},spyMap=new Map(spy.map((r,i)=>[dateOf(r),i])),modelByMonth=new Map(models.map(m=>[m.month,m]));for(const s of symbols)maps[s]=new Map(by[s].map((r,i)=>[dateOf(r),i]));let cash=CAPITAL,pos={},closed=[];for(let di=1;di<dates.length;di++){const date=dates[di],sigDate=dates[di-1];if(date<ANCHOR)continue;const model=modelByMonth.get(date.slice(0,7));if(!model)continue;for(const s of Object.keys(pos)){const idx=maps[s].get(date);if(idx==null)continue;const bar=by[s][idx],q=pos[s],p=q.params,age=di-q.entryDI,stop=q.entryRaw*(1-p.stop),target=q.entryRaw*(1+p.target);let raw=null,why='';if(bar.l<=stop){raw=stop;why='stop'}else if(bar.h>=target){raw=target;why='target'}else if(age>=p.hold){raw=bar.c;why='maxhold'}if(raw!=null){const exit=raw*(1-COST_SIDE),value=q.shares*exit,pnl=value-q.cost;cash+=value;closed.push({symbol:s,entryDate:q.entryDate,exitDate:date,entry:q.entry,exit,entryRaw:q.entryRaw,shares:q.shares,pnl,ret:exit/q.entry-1,why,modelMonth:q.modelMonth,modelHash:q.modelHash,params:q.params});delete pos[s]}}let cand=[],p=model.params;for(const s of symbols){if(pos[s])continue;const si=maps[s].get(sigDate),ti=maps[s].get(date);if(si==null||ti==null)continue;const a=by[s],need=Math.max(p.breakout+1,p.trend==='sma200'?205:p.trend==='sma100'?105:25,25);if(si<need)continue;const close=a[si].c,ph=prevHigh(a,si,p.breakout);if(!(ph&&close>ph))continue;if(p.trend!=='off'){const n=p.trend==='sma200'?200:100,sm=sma(a,si,n);if(!(sm&&close>sm))continue}if(p.volume!=='off'){const av=avgVol(a,si-1,20);if(!(av>0&&a[si].v>=av*p.volume))continue}if(p.regime!=='off'){const spi=spyMap.get(sigDate),n=p.regime==='spy200'?200:100;if(spi==null)continue;const ss=sma(spy,spi,n);if(!(ss&&spy[spi].c>ss))continue}const mom20=si>=20?close/a[si-20].c-1:0,breakoutPct=close/ph-1;cand.push({s,score:mom20+breakoutPct*2,open:a[ti].o})}cand.sort((a,b)=>b.score-a.score);while(cand.length&&Object.keys(pos).length<MAXPOS){const c=cand.shift(),mark=Object.values(pos).reduce((s,q)=>s+q.cost,0),eq=cash+mark,entryRaw=c.open,entry=entryRaw*(1+COST_SIDE),riskCash=eq*RISK,riskPerShare=Math.max(.0001,entryRaw*p.stop),shares=Math.min(riskCash/riskPerShare,(eq*MAXPOSPCT)/entry,cash/entry);if(!(shares>0))break;const cost=shares*entry;if(cost>cash)break;cash-=cost;pos[c.s]={symbol:c.s,entryRaw,entry,shares,cost,entryDate:date,entryDI:di,params:{...p},modelMonth:model.month,modelHash:model.selectedHash}}}const last=dates.at(-1),lastPrices={};if(last)for(const s of symbols){const idx=maps[s].get(last);if(idx!=null)lastPrices[s]=by[s][idx].c}const open=Object.values(pos).map(q=>({...q,lastPrice:lastPrices[q.symbol]||q.entryRaw,unrealized:q.shares*(lastPrices[q.symbol]||q.entryRaw)-q.cost}));return{closed,open,lastMarketDate:last,stats:calcStats(closed,open,lastPrices)}}
async function refresh(progress){let rows=[],providers=new Set(),end=today(),all=[...SYMBOLS,'SPY'];for(let i=0;i<all.length;i++){const s=all[i];progress?.(`Hämtar ${s} · ${i+1}/${all.length}`);const a=await getSymbol(s,TRAIN_START,end);a.forEach(r=>providers.add(r.provider));rows.push(...a)}rows.sort((a,b)=>a.t.localeCompare(b.t)||a.symbol.localeCompare(b.symbol));let x=load()||fresh();await ensureModels(rows,x,progress);x=load()||x;const oldN=x.closed?.length||0,oldDate=x.lastMarketDate,sim=simulate(rows,x.models);x.closed=sim.closed;x.open=sim.open;x.stats=sim.stats;x.lastMarketDate=sim.lastMarketDate;x.lastRefreshAt=new Date().toISOString();x.provider=[...providers].join(' → ');for(const m of [60,120,250])if(x.closed.length>=m)x.milestones[m]=true;if(oldN!==x.closed.length||oldDate!==x.lastMarketDate)x.history.push({at:x.lastRefreshAt,event:'REFRESH',marketDate:x.lastMarketDate,closed:x.closed.length,pl:x.stats.pl,pf:x.stats.pf,model:x.models.at(-1)?.selectedHash});if(x.history.length>80)x.history=x.history.slice(-80);return save(normalizeState(x))}
function stableNum(n){return Number(n||0).toFixed(8)}
function tradeId(t,kind='CLOSED'){
 const parts=[kind,String(t.symbol||''),String(t.entryDate||''),String(t.exitDate||''),stableNum(t.entryRaw??t.entry),stableNum(t.shares),String(t.modelMonth||''),String(t.modelHash||'')];
 return parts.join('|');
}
function normalizeState(x){
 if(!x)return x;
 x.closed=(x.closed||[]).map(t=>({...t,tradeId:t.tradeId||tradeId(t,'CLOSED')}));
 x.open=(x.open||[]).map(t=>({...t,tradeId:t.tradeId||tradeId(t,'OPEN')}));
 x.history=Array.isArray(x.history)?x.history:[];
 x.milestones=x.milestones||{60:false,120:false,250:false};
 return x;
}
function report(){const x=load()||fresh(),s=x.stats||{n:0,pl:0,pf:0,wr:0,dd:0,unrealized:0,open:0,equity:CAPITAL},f=n=>Number(n).toLocaleString('sv-SE',{maximumFractionDigits:2}),pct=n=>(100*Number(n)).toFixed(2)+'%';return ['LINAS OPTI – G3 REAL FORWARD / PAPER','Clean Core: V0.2.32','G3 Research Gate: PASS · FRYST','Planhash: '+PLAN_HASH,'Anchor: '+ANCHOR,'Handel: AVSTÄNGD','Kandidatpool: fryst topp 12 från 2020–2022 · månadsval endast med då känd data','',`Senaste marknadsdag: ${x.lastMarketDate||'ingen ännu'}`,`Stängda affärer: ${s.n} · P/L ${f(s.pl)} · PF ${f(s.pf)} · WR ${pct(s.wr)} · DD ${pct(s.dd)}`,`Öppna positioner: ${s.open} · orealiserat ${f(s.unrealized)} · modell-equity ${f(s.equity)}`,'',`Månadsmodeller: ${(x.models||[]).map(m=>m.month+' '+m.selectedHash+' (train '+m.trainEnd+')').join(' · ')}`,'',`Milstolpar: 60 ${x.milestones[60]?'✓':'—'} · 120 ${x.milestones[120]?'✓':'—'} · 250 ${x.milestones[250]?'✓':'—'}`,'','OBS: September 2026 använder det redan frysta G3-valet 69147890 tränat t.o.m. 2026-08-31. Framtida månadsval görs först när föregående månad finns i marknadsdatan. Ingen rescue/efteroptimering.'].join('\n')}
function download(text,name,type='text/plain'){const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)}
function exportReport(){download(report(),`LINAS_OPTI_G3_REAL_FORWARD_V0232_${today()}.txt`)}
function exportRaw(){download(JSON.stringify(load()||fresh(),null,2),`LINAS_OPTI_G3_REAL_FORWARD_RAW_V0232_${today()}.json`,'application/json')}
window.LinaG3ForwardEngine={VERSION,KEY,ANCHOR,PLAN_HASH,POOL,load,fresh,save,refresh,report,exportReport,exportRaw,tradeId,normalizeState};
})();
