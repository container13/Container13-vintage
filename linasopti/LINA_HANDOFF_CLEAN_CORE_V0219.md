# LINA HANDOFF – CLEAN CORE V0.2.19

Built from V0.2.18. UI-only Lina watermark restoration from the supplied legacy Linas Opti V0.58.8 package.

## Lina background
The effective legacy V0.20 + V0.21 watermark CSS blocks from `linasopti.css` are copied verbatim into Clean Core after the V0.2.18 rule, so they win the cascade exactly as in legacy. This restores the radial mask that fades the circular image edge, plus the legacy effective desktop/mobile size, opacity, top position, filter, card translucency and backdrop behavior.

`lina-circle.png` is byte-identical between legacy V0.58.8 and Clean Core (SHA256 `87d1d79e4cb1efb52cec0ce93a76c4b36010a1b8be3a4d57887649c11ff692e2`).

## Research
No G2/G3/G4/G5 strategy, plan, runner, gate, forward, data, cost, storage or parameter logic changed. G5 plan `6ec36eb2` and its already generated result remain untouched. Handel AV. Robotmognad 48/100.
